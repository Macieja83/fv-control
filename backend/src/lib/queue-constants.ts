export const PIPELINE_QUEUE_NAME = "fvcontrol-pipeline";

/** BullMQ queue + default job name (no `:` — BullMQ forbids colons in queue names). */
export const IMAP_ZENBOX_SYNC_QUEUE_NAME = "imap-sync-zenbox";
