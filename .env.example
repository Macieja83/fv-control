# Kopiuj do pliku `.env` (nie commituj `.env`).
# Hasło jest sprawdzane TYLKO po stronie serwera Vite / produkcyjnego API — nie trafia do bundla frontu.

FV_RESTA_LOGIN_PASSWORD=ustaw_silne_haslo

# Opcjonalnie: jeśli ustawione, zaloguje się tylko ten adres (bez tego — dowolny poprawny e-mail).
# FV_RESTA_LOGIN_EMAIL=manager@resta.biz
